{
  "name": "calculadora-mktplace",
  "private": true,
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test \"tests/unit/*.test.js\"",
    "test:e2e": "playwright test",
    "build": "rm -rf _site && mkdir -p _site/vendor && cp index.html calc.js _site/ && cp vendor/*.js _site/vendor/"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "serve": "14.2.4"
  }
}
